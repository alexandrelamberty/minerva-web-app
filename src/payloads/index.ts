export interface TalkRequestPayload {
  message: string;
}

export interface TalkEventPayload {
  username: string;
  avatar: string;
  timestamp: string;
  message: string;
}

export interface YellPayload {
  message: string;
}
